<project>
  <modelVersion>4.0.0</modelVersion>
  <!-- 
  	IMPORTANT:
  		To deploy use "deploy:deploy-file" Goal
  		ie: 
  		>mvn deploy:deploy-file
  		
  		using "deploy" Goal will result in double deploying,
  		not acceptable for Release Versions.
  		Code for this binding is commented to avoid this problem
   -->
  <parent>
  	<groupId>org.universAAL.ui</groupId>
  	<artifactId>ui.pom</artifactId>
  	<version>1.0.0</version>
  </parent>
  
  <artifactId>ui.handler.gui.tableLayout</artifactId>
  <version>1.0-SNAPSHOT</version>
  
  <build>
  		<defaultGoal>install:install-file</defaultGoal>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<version>2.6</version>
				<goals>
        			<goal>deploy-file</goal>
    			</goals>
				<configuration>
                    <groupId>${project.groupId}</groupId>
                    <artifactId>${project.artifactId}</artifactId>
                    <version>${project.version}</version>
                    <packaging>jar</packaging>
                    <file>TableLayout.jar</file>
                    <pomFile>pom.xml</pomFile>
                    <url>${project.distributionManagement.snapshotRepository.url}</url>
                    <repositoryId>${project.distributionManagement.snapshotRepository.id}</repositoryId>
                </configuration>
                <!-- 
                <executions> 
	                <execution>
		              <id>deploy file</id>
		              <phase>deploy</phase>
		              <goals>
		                <goal>deploy-file</goal>
		              </goals> 
		            </execution>
          		</executions>
          			 -->
			 </plugin>
			 <plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-install-plugin</artifactId>
				<version>2.3.1</version>
					<goals>
	        			<goal>install-file</goal>
	    			</goals>
					<configuration>
	                    <groupId>${project.groupId}</groupId>
	                    <artifactId>${project.artifactId}</artifactId>
	                    <version>${project.version}</version>
	                    <packaging>jar</packaging>
	                    <file>TableLayout.jar</file>
	                    <pomFile>pom.xml</pomFile>
	                    <repositoryLayout>default</repositoryLayout>
	                </configuration>
	                <executions> 
	                <execution>
		              <id>install file</id>
		              <phase>install</phase>
		              <goals>
		                <goal>install-file</goal>
		              </goals> 
		             </execution>
          			</executions> 
			 </plugin>
		</plugins>
  </build>
</project>
